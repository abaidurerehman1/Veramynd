# normalize_lesson prompt

The prompt that runs in production is the packaged file:

`veramynd_parser/veramynd_parser/prompts/normalize_lesson.md`

This docs copy used to be a byte-identical duplicate; it was replaced with
this pointer so there is exactly one source of truth (a stale docs copy of a
prompt is worse than none — it silently diverges from what the model sees).
